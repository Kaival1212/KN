/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['knconsulting.uk', 'images.unsplash.com'],
    },

    swcMinify: true,


};

export default nextConfig;
